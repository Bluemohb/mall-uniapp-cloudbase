# 开发环境配置（pnpm dev:*）—— Vite / uni-app 以 mode=development 加载本文件
#
# 全局 Mock 开关：true = 商品列表 / 首页推荐 / 搜索 / 商品详情读本地 mock
VITE_USE_MOCK=true

# 订单专用 Mock 开关：false = 订单直连云端 orders 集合
# 这样可以直接用控制台里的真实订单调试「下单 → 列表 → 详情」；
# 想在本地演示下单闭环、不依赖云端时改成 true。
VITE_ORDER_MOCK=false

# 支付模式：mock = 强制模拟支付
# 个人主体小程序开不了微信支付（需企业主体 + 认证），商户凭证永远配不上；
# 而客户端「能不能调起支付」的探测条件在个人号里全为真，不显式声明就会给出
# 一个点了必然失败的「立即支付」按钮。将来配好凭证改成 auto 即可，代码不用动。
# 详见 README「支付模式开关」。
VITE_PAY_MODE=mock
